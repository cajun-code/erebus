#! /usr/bin/env ruby
require "bundler/setup"

path = File.absolute_path(File.join(File.dirname(__FILE__), "..", "lib"))

$LOAD_PATH << path

require "cpp_dev_tool"

CppDevTool::App.load_generators
CppDevTool::App.start(ARGV)